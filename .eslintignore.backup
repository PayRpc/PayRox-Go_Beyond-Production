# ESLint ignore patterns for intentional coding patterns
# These files use legacy require() statements and patterns for compatibility

# Legacy Node.js require patterns (intentional) -- now linted under zero-issue config

# Empty catch blocks for graceful error handling (intentional)
scripts/analysis/regression-harness-gen.js
scripts/diagnose-manifest-mismatch.js
tools/generate-manifest.js

# Nested functions for modularity (intentional)
scripts/extract_selectors.js

# TypeScript ignore comments for runtime flexibility (intentional) -- now allowed via zero-issue config

# Build artifacts and generated files
artifacts/
cache/
dist/
node_modules/
typechain-types/
