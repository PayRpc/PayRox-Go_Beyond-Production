{
  // 🔗 IDE COMPATIBILITY STUB
  // This file exists solely for IDE/editor TypeScript language service compatibility.
  // All actual compilation uses tsconfig.hardhat.json as the canonical configuration.
  "extends": "./tsconfig.hardhat.json"
}
